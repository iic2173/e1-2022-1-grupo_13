
import PositionIndex from '../../components/Positions/PositionIndex';

const UserPositionsList = () => {

    return(
        <section className="container">
            <PositionIndex />
        </section>
    )
};

export default UserPositionsList;